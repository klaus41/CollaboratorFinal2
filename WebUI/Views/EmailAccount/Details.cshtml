@model WebUI.Models.EmailAccount

@{
    ViewBag.Title = "Details";
}
<div class="box">
    <div class="box-header with-border">
        <h3 class="box-title">@Html.DisplayFor(Model => Model.EmailAddress) - Detaljer</h3>
        <div class="box-tools pull-right">
            <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip" title="Collapse">
                <i class="fa fa-minus"></i>
            </button>
            <button type="button" class="btn btn-box-tool" data-widget="remove" data-toggle="tooltip" title="Remove">
                <i class="fa fa-times"></i>
            </button>
        </div>
    </div>
    <div class="box-body">
        <table class="table table-bordered table-stibed dataTable">
            <thead>
                <tr role="row">
                    <th>
                        Email
                    </th>
                    <th>

                    </th>
                </tr>
            </thead>

            <tbody>

                <tr role="row">
                    <td>
                        @Html.DisplayFor(model => model.EmailAddress)
                    </td>
                    <td align="right">
                        <a href="@Url.Action("Edit", "EmailAccount",  new { id = Model.ID })" class="btn btn-info ">Tilpas</a>
                    </td>
                </tr>
            </tbody>
        </table>
        <div>
            <a href="@Url.Action("Index", "EmailAccount")" class="btn btn-info pull-right" style="margin-top:10px">Tilbage</a>
        </div>
    </div>
    </div>